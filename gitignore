!tags/
*.pyc
*.sw[nop]
.DS_Store
.bundle
.byebug_history
.env
.git/
/bower_components/
/log
/node_modules/
/tmp
/vendor
db/*.sqlite3
log/*.log
rerun.txt
tags
tmp/**/*

# Written by tooling into the global excludes; kept here so nothing appends to
# ~/.gitignore at runtime — that file is a symlink into this repo, and an append
# would dirty the checkout and fail the next converge.
app.env
**/.claude/settings.local.json
